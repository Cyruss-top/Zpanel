# Vue 3 前端（v0.3.0 初始化）

技术栈：Vue 3 + Vite + TypeScript + Naive UI + Pinia

开发文档见 `docs/DEVELOPMENT.md` §4、§8。

```bash
# v0.3.0 时执行
npm create vite@latest . -- --template vue-ts
npm install naive-ui pinia vue-router @vueuse/core echarts vue-echarts
npm run dev
```
